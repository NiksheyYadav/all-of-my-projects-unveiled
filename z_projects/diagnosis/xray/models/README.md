# Models Directory

This directory contains the trained models for the X-ray classification project. The models are saved in the `.keras` format and can be loaded using TensorFlow/Keras for prediction and evaluation purposes.
